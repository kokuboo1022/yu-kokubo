# 刺繍屋さん お問い合わせ・自動見積もりフォーム — 仕様書

> このドキュメントは「実装前の仕様」をまとめた生きた仕様書です。
> ヒアリングで決まったことを随時ここに追記・修正し、固まった内容をもとに実装します。
> **現時点ではコードは実装しません。** まずこの仕様を確定させることが目的です。

最終更新: 2026-05-30

---

## 1. 背景・目的

- 刺繍屋さん（人形屋の派生事業）のお問い合わせを自動化したい。
- 現状は Instagram → LINE 誘導 → LINE で個別にやり取りしており、毎回のリレーが負担。
- LINE 公式アカウントは無料/低プランだと過去のやり取りが数日で消えるため、
  **問い合わせ内容・画像・回答金額の履歴をデータベースに残したい**。
- Google フォームは「冷たい感じがする」ため不採用。**ちゃんとしたサイトの「フォーム」**として、
  入力する側がなるべく簡単に操作できる体験にしたい。

### 進め方の方針
- まずは「こんな感じ」というデモを **Firebase Hosting** に公開し、ブラウザから触ってもらう。
- デモのコード／データはそのまま正式リリースに流用する前提。
- **リリース後に仕様変更が入る前提**なので、料金・選択肢・項目を
  **データ駆動でフレキシブルにカスタマイズできる構成**で母体コードを書く（後述「12. 実装方針」）。

---

## 2. 提供メニュー（カテゴリ）

トップでお客様が最初に選ぶカテゴリ:

1. 道着の刺繍（文字入れ） … その場で**概算金額あり**
2. アイテム・衣類へロゴ／イラスト挿入 … （未設計・後日連絡想定）
3. おなまえスタイ（出産祝い） … 設計済み
4. 子供のらくがき刺繍化 … **金額表示なし／後日連絡**
5. レーザーカット（木製品など） … （未設計）

> 本デモでは **①道着 / ③おなまえスタイ / ④お絵かき刺繍化** の3つを作り込む。
> ②ロゴ挿入・⑤レーザーカットは枠だけ用意し、後日詳細を詰める。

---

## 3. 共通項目（全カテゴリ共通で聞く）

- お名前
- メールアドレス（後日連絡用）
- ご希望の納期（下記・選択式）
- 備考・自由記述

### 納期の聞き方
- **基本は選択式（B案）**: 例「お急ぎ（1週間以内）/ 2〜3週間 / 1ヶ月以上 / 相談」
- 加えて **自由記述欄**: 「イベントに間に合わせたい」など明確な事情があれば記載してもらう。

### 連絡フローの考え方
- 基本はフォームの返信先メールアドレスに返信（一般的で分かりやすい）。
- 理想は LINE 経由で来た人には LINE で返事（親切）。
- 二段構え: **メールは必ず取得 / 返信は状況に応じて LINE も可**。
- 将来 LINE ログイン連携などで「LINEでそのまま返信」へ発展可能（段階的に）。

---

## 4. ① 道着の刺繍（文字入れ）

### 共通設定（1回だけ選ぶ・全箇所に反映）
- **道着の種類**: 柔道着 / 空手着
- **フォント**: 毛筆体 / 楷書体 / ゴシック体 / 丸ゴシック（仮。日本語Webフォントで近いものを使用）
- **色**: 白 / 黒 / 赤 / 金（仮）

### 場所ごとの設定（複数選択可・場所ごとに個別入力）
- **場所**: 背中 / 襟（衿元）/ 袖 / 胸
- **文字**: 場所ごとに自由入力。**上限8文字**。スペース・改行はカウントしない。
- **サイズ**: S / M / L（寸法目安＋説明ラベル付き、場所ごとに選択）

| 表示 | 寸法の目安（仮） | 説明ラベル | 料金補正（仮） |
|------|----------------|-----------|--------------|
| S | 約 3cm × 3cm | 控えめ・ワンポイント | −100円 |
| M | 約 5cm × 5cm | 標準・一般的 | ±0円 |
| L | 約 8cm × 8cm | 大きめ・目立つ | +200円 |

> サイズは本来「○cm × ○cm」の寸法で料金・糸量が変わるが、一見さんには分かりにくいため
> 便宜上 S/M/L のラベルで提示。将来「実寸入力」モードを追加できる余地を残す。

### プレビュー
- 道着のサンプル画像に、入力文字・フォント・色を**リアルタイム反映**。
- 方式A（Webフォントを画像に重ねる）で実装。刺繍風テクスチャ再現（方式B）は将来検討。

### 概算見積もりロジック（すべて仮の数値）
- 場所ごとの基本料金: 背中 +1,000 / 襟 +500 / 袖 +500 / 胸 +500
- 文字料金: 1文字 500円 ×（その場所の文字数）
- サイズ補正: S −100 / M ±0 / L +200
- 場所ごとに計算し、**合計を概算として表示**。
- 注記を必ず添える: 「※あくまで概算です。正式なお見積もりは後日ご連絡します。」

**計算例**: 背中「山田太郎」(3文字, L) ＋ 襟「柔道部」(3文字, M)
- 背中 = 1,000 + 3×500 + 200 = 2,700円
- 襟　 = 500 + 3×500 + 0 = 2,000円
- 合計 = **4,700円（概算）**

---

## 5. ③ おなまえスタイ（出産祝い）

選ぶだけで仕様が決まる簡単なメニュー。

### 入力項目
1. **ベースのスタイを選択**: サンプル画像の中から選ぶ。
   - 現状サンプル画像が無いため、**プレースホルダーのグレー画像**で用意。
   - 「画像を選べる」ことが視覚的に分かるUI（サムネイル一覧から選択、選択中が分かる枠/チェック表示）。
2. **文字色**: 原色系を3〜4色（仮。例: 赤 / 青 / 黄 / 緑 など）。
3. **入れる文字（お名前）**: ひらがな・漢字・ローマ字いずれもOK。上限8文字（仮）。

### 金額表示
- **未確定**（要相談）。道着同様その場で概算を出すことも可能。デフォルトは後日連絡で仮置き。

---

## 6. ④ 子供のらくがき刺繍化

工数（手動でのデータ化）が読めないため、**その場で金額は出さず後日連絡**。

### 入力項目（この3つ）
1. **ファイルのアップロード**: 描いた絵の画像 **1枚**。形式 = JPG / PNG / HEIC。
2. **仕上がり**: 白黒 / カラー / おまかせ（3択）。
3. **何に入れるか（媒体）**: 1つだけ選択。

| 媒体 | ベース料金（仮・※表示はしない） |
|------|------------------------------|
| お絵かきボード（布に描いて飾る） | 4,000円 |
| 帽子 | 2,000円 |
| トートバッグ | 2,500円 |
| フォトフレーム | 3,000円 |
| シャツ | 3,000円 |

> ベース料金は内部参考値。**お客様には金額を表示しない**（デザイン/データ化費が都度変わるため）。
> 送信後は「内容を確認のうえ、後日ご連絡いたします」と表示。

---

## 7. ② ロゴ／イラスト挿入（未設計・メモ）

- お客様が**デザインデータを持ち込む**前提。店側でデザインは行わない。
- 持ち込みサンプルを元に刺繍製品として仕上げる。
- データの複雑さで工数が変わるため、おそらく**後日連絡**型。
- 詳細は後日ヒアリング。

## 8. ⑤ レーザーカット（未設計・メモ）

- 木製品などに文字・ロゴを入れられる。詳細は後日ヒアリング。

---

## 9. 送信後の流れ

- 入力内容＋アップロード画像を**データベースに保存**。
- 保存したい履歴: 問い合わせ内容 / 画像データ / **いくらで回答したかの金額履歴**。
- お客様への自動返信・店側への通知は次フェーズで設計（メール通知が第一候補）。

---

## 10. データ保存・バックエンド候補

| 方法 | メリット | デメリット |
|------|---------|-----------|
| Firebase (Firestore + Storage) | 画面・画像・DBを1つで完結。デモと同じ基盤 | 管理画面は自作が必要 |
| Airtable | 非エンジニアでも扱いやすい。表＋画像添付 | 大量だと有料。未経験（試してみたい） |
| Supabase | 無料・画像ストレージ・管理画面内蔵 | やや技術的 |

- **デモ段階**: Firebase に保存、または Airtable に送信。どちらでも開始可能。
- ユーザーは **Airtable を試してみたい** 意向あり → 管理画面として相性◎。
- 正式版でも Firebase ベースで作っておけばコード・データを流用可能。

---

## 11. ホスティング・公開

- **Firebase Hosting** にデプロイし、ブラウザから直接使えるデモとして公開。
  （過去のセットリストアプリ／朝タスクアプリと同じ運用）
- 用語整理:
  - Firebase **Hosting** = フォーム画面（Webサイト）の公開先。
  - Firebase **Storage** = アップロード画像の保存先。
  - Firebase **Firestore** = 問い合わせデータ（テキスト/金額履歴）の保存先。
- 刺繍屋の独自メインサイトは未整備（本業の人形屋サイトの下層に刺繍ページがある状態、ECあり）。
  まずは「どんなサービスができるか」を見せ、操作してもらえる状態を目指す。

---

## 12. 実装方針（フレキシブルにカスタマイズできる母体）

リリース後にコードを直接触って仕様変更する前提のため、以下を徹底する:

- **既存スタック踏襲**: React 19 + Vite + Firebase Hosting（現リポジトリ構成）。
- **データ駆動設計**: メニュー・選択肢・料金ルールを JSX にベタ書きせず、
  設定オブジェクト（例 `src/config/` の JSON/JS）に切り出す。
  - カテゴリ定義、各カテゴリの質問項目、選択肢、料金ルールを設定ファイルで管理。
  - 料金計算は「設定値 → 計算関数」の形にし、数値変更だけで料金改定できるようにする。
- **条件分岐の可変表示**: 「この項目を選んだらこの入力を出す」を設定で表現できる仕組み。
- **文言・上限値（8文字など）も定数化**して差し替えやすくする。
- 画像（サンプル道着・スタイ）は差し替え前提でプレースホルダーを使う。

---

## 13. 未決定・要確認リスト

- [ ] 道着・おなまえスタイの**実際の料金表**（現在はすべて仮の数値）。
- [ ] おなまえスタイの**スタイのラインナップ**（種類・色）と**文字色の確定**。
- [ ] おなまえスタイで**その場見積もりを出すか／後日連絡か**。
- [ ] 場所ごとの基本料金・サイズ補正の確定（道着）。
- [ ] フォント・色の実際のラインナップ。
- [ ] 店側への通知方法（メール / LINE / 管理画面）。
- [ ] データ保存先の最終決定（Firebase / Airtable）。
- [ ] 独自ドメイン・公開URLの設計。
- [ ] ②ロゴ挿入・⑤レーザーカットの詳細仕様。
